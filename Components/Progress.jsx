import React from 'react'

export default function Progress(props) {
  return (
    <div>
        
<progress>id={props.id}
text:{props.text}
value={props.value}
max={props.max}
name={props.name}
      
 </progress>
      
    </div>
  )
}
